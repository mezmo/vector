features:
- mezmo-aggregate-distributed-integration-tests

test_filter: "::mezmo_aggregate_distributed::"

env:
  MEZMO_STATE_CONNECTION_STRING: redis://dragonfly:6379/0

matrix:
  version: [latest]
paths:
- "src/transforms/mezmo_aggregate_distributed/**"
- "scripts/integration/mezmo-aggregate-distributed/**"
